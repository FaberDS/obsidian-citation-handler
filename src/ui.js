import { $ } from "./utils.js";
export const ui = {
  openBtn: $("open"),
  statusEl: $("status"),
  filterEl: $("filter"),
  filesEl: $("files"),
  metaEl: $("meta"),
  contentEl: $("content"),
  llmOutputEl: $("llmOutputElement"),
  promptBtn: $("promptCitations"),
};
